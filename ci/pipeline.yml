resources:
  - name: git-repo
    type: git
    source:
      uri: ((github-repo))
      branch: ((branch))

jobs:
  - name: run-build
    plan:
      - get: git-repo
        trigger: true
      - task: build
        file: git-repo/ci/tasks/build.yml

