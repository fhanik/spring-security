---
platform: linux
image_resource:
  type: docker-image
  source:
    repository: openjdk
    tag: ((openjdk-tag))
inputs:
  - name: git-repo
outputs:
  - name: git-repo
run:
  path: ci/tasks/gpg-create-signing-keyring.sh
  args: [{{public-key}}, {{private-key}}, {{pass-phrase}}]
  dir: git-repo

